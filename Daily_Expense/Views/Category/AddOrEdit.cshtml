@model Daily_Expense.Models.Category


@{
    ViewData["PageTitle"] = Model.CategoryId ==0?  "Create a new Category" : "Edit Category";
}



<h1>Create</h1>

<h4>Category</h4>
<hr />
<div class="row">
    <div class="col-md-7 style=">
        <div class="widget p-5 ">
            <form asp-action="AddOrEdit" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" name="CategoryId" asp-for="@Model.CategoryId" value="@Model.CategoryId" />
                <div class="form-group">
                    <div class='e-btn-group custom-rtb-group'>
                        <input type="radio" id="radioleft" name="Type" value="Expense" checked="@(Model.Type=="Expense")" />
                        <label class="e-btn" for="radioleft">Expense</label>
                        <input type="radio" id="radiomiddle" name="Type" value="Income" checked="@(Model.Type=="Income")" />
                        <label class="e-btn" for="radiomiddle">Income</label>
                    </div>
                    <span asp-validation-for="Type" class="text-danger"></span>
                </div>
                <br />

                <div class="control_wrapper TextBox-control-section">
                    <ejs-textbox id="Title" asp-for="Title" placeholder="Title" ejs-for="Title" floatLabelType="Always"></ejs-textbox>
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>
                <div class="control_wrapper TextBox-control-section">
                    <ejs-textbox id="Icon" asp-for="Icon" placeholder="Icon" ejs-for="Icon" floatLabelType="Always"></ejs-textbox>
                    <span asp-validation-for="Icon" class="text-danger"></span>
                </div>
                @if (Model.CategoryId == 0)
                {
                    <div class="form-group mt-2">
                        <ejs-button type="submit" id="successbtn" cssClass="e-success" content="Create"></ejs-button>

                    </div>
                }
                else
                {
                    <div class="form-group mt-2">
                        <ejs-button type="submit" id="successbtn" cssClass="e-primary" content="Update"></ejs-button>

                    </div>
                }

                
            </form>
        </di>

        
    </div>

</div>
    <div class="col-md-5">
        <div class="widget h-100 d-flex justify-content-center align-items-center" style="background-color:slategray">
            <i class="fa-solid fa-shapes fa-2xl"></i>
        </div>

    </div>


<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
